<h1>Workout Plans</h1>

<div class="container">
  <% @workouts.each do |workout| %>
    <ul>
      <li><%= link_to workout.name, workout %></li>
    </ul>
  <% end %>
</div>

<div>
    <h3>
      <%= link_to 'Create a Workout Plan', new_workout_path %>
    </h3>
</div>
